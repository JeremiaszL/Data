import pandas as pd

url_bc = "https://raw.githubusercontent.com/mwaskom/seaborn-data/master/breast_cancer.csv"
df_bc = pd.read_csv(url_bc)

rows, cols = df_bc.shape
print(f"Breast Cancer: {rows} próbek, {cols} kolumn")

print("Kolumny:", df_bc.columns.tolist())

if 'diagnosis' in df_bc.columns:
    print(df_bc['diagnosis'].value_counts())
